#2024年12月3日
#### 尝试通过bun启动tauri
要求安装vite：bun add -D vite
相比cargo rust版本，vite版能直接在浏览器启动。
vite是即时预览引擎，而编译才会编译。
共4.57GB，空文件。

结果exe只有10MB，可以接受。
可是，如果可能的话，我仍然希望使用floem...为什么？
我将它交由命运：如果一次安装floem成功，那么卸载bun等，改用floem；如果失败，那么使用typescript和tauri。
它成功了，和上次不一样。

卸载：Svelte for VS Code
（即便用bun也应该卸载）Prettier
（无关）Even Better TOML