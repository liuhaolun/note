我们意识到：程序具有有序列表来表示‘FLOW’，将围绕FLOW设计程序。
**1.MAIN入口点，初始化{GUI::HOME；Data::Config<-从本地文件读取}**
1.1HOME菜单包括：
1.1.1顶栏菜单{串口，配置（控制器）->滑台，数据（读入神经网络权重、已存储数据图，自动存储的说明，触发存储）}
	`slideConfItem=confMenu.Append(-1,"","")`不手动指定id, 名称，底栏说明
	`self.Bind(wx.EVT_MENU,self.OnSlideConf,slideConfItem)`绑定一个event(wx.EVT_MENU)到OnSlideConf函数，
	1.1弹出菜单：
		`class SlideConfFrame, def __init__(self,parent)`parent的意义是：避免主窗口关闭后，子窗口不自动销毁`self.slideConfWindow=SlideConfFrame(self)`self作为parent，`super().__init__(parent)`使用了parent后，才能确保自动销毁。
	1.2弹出窗口尺寸
		`panel.SetSizerAndFit(sizer) curSize=self.GetBestSize() #self.Fit() self.SetSize((curSize[0]+20,curSize[1]+20))`Fit会缩窄窗口到BestSize, 获取该值并扩大后，进行SetSize能够提供合适的边框留白（边框距离，空白，边距）
1.1.2数据控制和滑台控制，神经网络输出结果及训练数据控制，
	在wxpython中，tab是notebook中的多分栏。我们否决了多分栏策略，选择密铺大量按钮，不使用notebook.
	2.1Z轴步进区域
	10μm，左右分别是正负10μm；100μm
1.1.3右侧不需要显示神经网络输出结果，只需要graphs显示散点图及当前的各种数据，通过分tab避免数据堆叠，分为‘散点图’和‘普通数据’两个tab，数据本身并不分散不需要归拢。
	效果：{多行表现最新的数据组（位置-力）}和{散点图}在两个tab
1.2Config内容。Config使用toml存储为key-value对。
	移动步长，延时--硬编码数据不参与，可以调整的应该满足{用户感知度，具有灵活性，不能确定某一数值一定好}
1.2.1串口数据
1.2.2运动速度、加速度、S曲率
MAIN=subClass<|wx.Frame::left_panel=0.3+right_panel=0.7
	left_panel::tab_manual=wx.Panel()
	right_panel::tab_graphs+tab_visual=wx.Panel()
	frame的展示需要：SetSize((1440,900)) Centre() Show(), frame不需要作为类，而可以self.调用自身
**2.用户进行配置，控制程序初始化**