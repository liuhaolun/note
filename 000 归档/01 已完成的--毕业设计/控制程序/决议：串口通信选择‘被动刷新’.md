怎样的串口通信好用呢？-->我在考虑‘需要偶尔刷新 or 只在调用时刷新’---调用也就是位置移动的时候，离散的时间步
由于：只在调用时刷新就可以了，所以：建立异步方法徒增理解成本。
【需求】在三个弹出窗口{滑台、传感器（共七个）、电感测微仪}分别配置串口{3,4,1+3}，能够‘B::测试’发送命令并解析返回信号。
因此：进行稀疏的数据记录（散点）。即‘步-数据’，然而扰动需要【测量：】多少ms消除
- 增加一个‘B::可选每1s/3s/5s刷新一次’
- 增加‘命令堆栈’传给滑台的命令是一个一个积累，一个一个消耗的。因此存在‘Data::结束位置’显示在用户界面。