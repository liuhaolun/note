https://www.youtube.com/watch?v=HyW54cI8Qdo
在完成后，我将把它调整为C++代码
P1.对齐版本，使用blender4.2而不是bforartists4.3。呃，下载错了，4.3.2也行吧。
	学生要有学生的样子，请使用blender4.2
	找不到‘转换为mesh curve’就此解决

- blender需要应用变化（ctrl+a），几何节点::填充曲线只有x-y平面能填充
- 在右上角-大纲视图，切换到‘未使用的数据’，删除其中‘新建后未能使用到的几何节点’
- shift+右键，增加线上节点（理线）
###
P2.把svg导入后，转换为mesh curve，将其用作另一个object::一般用立方体、的输入，通过‘物体信息’节点
P2.1对曲线重采样，分为两组：1.边缘：‘曲线转换为点’ 2.‘填充’-‘分布点于面上’。设置为不同的点半径。
P2.2‘存储已命名属性’，以划分两组，共后面使用。使用‘布尔’，勾选‘值’
P2.3合并后，连接到‘模拟’以在动画中每帧计算。计算‘设置位置’
![[../plus/20250316 使点震荡，存在逃逸和被夹在中间震荡.png]]
P2.4处理逃逸点，通过再次‘设置位置’到‘采样最近表面’
###
P3.将帧计算转为固定计算次数，使用‘重复区’节点

### 把点连接到所有点
P1.for each element节点，接受几何体输入，把每个index依次处理。节点左侧灰色socket可以接入‘希望使用的每个index独立数据’。
P2.使用嵌套的‘for each element’节点，以对于每个点连接到每个点。逻辑运算（当前选中两点的距离）应该在内侧循环中。
P2.1通过‘Distance’计算向量距离，通过‘Greater Than’和‘Switch’判断，在‘Switch’的‘True/false’输入希望‘视boolean情况而定的分支’。
&然而，我直接‘套用后先看看能否使用’这种做法极其错误，导致内存爆炸。
P3.我的格栅：3mm间距。那么，横向最大是10/0.3=33，纵向2.54/0.3=8.4，总量是33×33×8。有点大，我希望减少的话，横向不能超过纵向1倍。1.5厘米-1.5厘米-0.8厘米

### 通过remesh和wireframe修改器
P1.制作圆柱体（不能是：圆平面，填充)。
P1.1分离，挤出。
..并没有内部线框->(不行：P1.2勾选Dissolve Orthogonal Edges以避免空心)
